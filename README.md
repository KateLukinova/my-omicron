# Nuxt Minimal Starter

# My Omicron

Современное веб-приложение, созданное с использованием Nuxt 3, Vue 3, Tailwind CSS и Storybook для демонстрации компонентного подхода в разработке интерфейсов.

## 🚀 Технологии

- **Nuxt 3** - Полнофункциональный фреймворк на основе Vue.js
- **Vue 3** - Прогрессивный JavaScript фреймворк с Composition API
- **Tailwind CSS** - Utility-first CSS фреймворк
- **Storybook** - Инструмент для разработки UI компонентов в изоляции
- **TypeScript** - Типизированный JavaScript для повышения качества кода

## 📦 Установка

Склонируйте репозиторий и установите зависимости:

```bash
# Установка зависимостей
npm install
```

## 🛠️ Запуск проекта

### Разработка

Запуск сервера разработки:

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

### Storybook

Запуск Storybook для просмотра компонентов:

```bash
npm run storybook
```

Storybook будет доступен по адресу `http://localhost:6006`

### Продакшн

Сборка приложения для продакшна:

```bash
npm run build
npm run preview
```

## 🎨 Компоненты

### BaseButton

Универсальная кнопка с различными вариантами стилизации:

- **Варианты**: `primary`, `secondary`, `outline`, `ghost`
- **Размеры**: `sm`, `md`, `lg`  
- **Опции**: `disabled`, `rounded`

```vue
<BaseButton variant="primary" size="md" @click="handleClick">
  Текст кнопки
</BaseButton>
```

### BaseCard

Карточка для отображения контента с поддержкой header и footer:

- **Тени**: `none`, `sm`, `md`, `lg`, `xl`
- **Опции**: `rounded`, `bordered`, `hover`
- **Слоты**: `header`, `default`, `footer`

```vue
<BaseCard title="Заголовок" hover>
  <p>Основное содержимое карточки</p>
  <template #footer>
    <BaseButton size="sm">Действие</BaseButton>
  </template>
</BaseCard>
```

## 📖 Страницы

- **Главная** (`/`) - Демонстрация всех компонентов с интерактивными примерами
- **О проекте** (`/about`) - Информация о технологиях и возможностях проекта

## 🎯 Storybook Stories

В проекте настроены подробные истории для каждого компонента:

- `BaseButton.stories.js` - Все варианты кнопок
- `BaseCard.stories.js` - Различные конфигурации карточек

Каждая история включает:
- Настраиваемые контролы
- Автодокументацию
- Интерактивные примеры

## 🔧 Конфигурация

### Tailwind CSS

Настройки находятся в `tailwind.config.js`:
- Кастомная цветовая палитра
- Настройка путей для сканирования классов
- Поддержка всех файлов проекта и Storybook

### Storybook

Конфигурация в `.storybook/`:
- `main.js` - Основные настройки и аддоны
- `preview.js` - Подключение стилей и параметры

## 📝 Разработка

### Добавление новых компонентов

1. Создайте компонент в `components/`
2. Добавьте типизацию с помощью TypeScript
3. Используйте Tailwind CSS для стилизации
4. Создайте соответствующий `.stories.js` файл
5. Документируйте компонент в README

### Стилизация

Проект использует Tailwind CSS с кастомными настройками:
- Основная цветовая схема: `primary-*`
- Responsive дизайн
- Utility-first подход

Look at the [Nuxt documentation](https://nuxt.com/docs/getting-started/introduction) to learn more.

## Setup

Make sure to install dependencies:

```bash
# npm
npm install

# pnpm
pnpm install

# yarn
yarn install

# bun
bun install
```

## Development Server

Start the development server on `http://localhost:3000`:

```bash
# npm
npm run dev

# pnpm
pnpm dev

# yarn
yarn dev

# bun
bun run dev
```

## Production

Build the application for production:

```bash
# npm
npm run build

# pnpm
pnpm build

# yarn
yarn build

# bun
bun run build
```

Locally preview production build:

```bash
# npm
npm run preview

# pnpm
pnpm preview

# yarn
yarn preview

# bun
bun run preview
```

Check out the [deployment documentation](https://nuxt.com/docs/getting-started/deployment) for more information.
