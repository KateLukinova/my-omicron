# Визуальное регрессионное тестирование через Chromatic

## Что такое визуальное тестирование

Визуальное регрессионное тестирование позволяет автоматически обнаруживать нежелательные изменения в UI компонентов путем сравнения скриншотов (снимков) до и после изменений кода.

## Установка и настройка

### 1. Chromatic уже установлен в проекте

```bash
# Chromatic установлен как dev dependency
npm install # если еще не установлены зависимости
```

### 2. Получение токена проекта

1. Зарегистрируйтесь на [chromatic.com](https://chromatic.com)
2. Создайте новый проект или подключите существующий GitHub репозиторий
3. Скопируйте Project Token из настроек проекта
4. Замените `<your-project-token>` в файлах:
   - `package.json` (в скрипте "chromatic")
   - `chromatic.config.json` (в поле "projectToken")

### 3. Настройка переменных окружения (рекомендуется)

Создайте файл `.env.local` в корне проекта:

```bash
CHROMATIC_PROJECT_TOKEN=your-actual-token-here
```

Затем обновите `package.json`:

```json
"chromatic": "npx chromatic --project-token=$CHROMATIC_PROJECT_TOKEN"
```

## Использование

### Запуск визуального тестирования

```bash
# Полный процесс: сборка Storybook + запуск Chromatic
npm run test:visual

# Или только Chromatic (если Storybook уже собран)
npm run chromatic
```

### Первый запуск

При первом запуске Chromatic создаст базовые снимки (baseline) всех ваших stories. Это займет некоторое время.

### Последующие запуски

Chromatic будет сравнивать новые снимки с базовыми и покажет:
- ✅ **Accepted**: Изменений нет или они одобрены
- 🔍 **Changes detected**: Обнаружены визуальные изменения
- ❌ **Denied**: Изменения отклонены

## Конфигурация для stories

### Базовые параметры Chromatic в stories

```typescript
export const MyStory: Story = {
  // ... story configuration
  parameters: {
    chromatic: {
      // Тестирование на разных разрешениях
      viewports: [320, 768, 1200],
      // Задержка перед созданием снимка (мс)
      delay: 1000,
      // Отключить снимки для story
      disable: false,
      // Принудительно создать снимок даже если нет изменений
      forcedColors: 'active'
    }
  }
}
```

### Пример настроенной story

Посмотрите на `stories/Card.stories.ts`, story `FixedWidth` - там уже настроены параметры для тестирования на трех разрешениях.

## Workflow в команде

### 1. Разработка компонентов

1. Создайте или измените компонент
2. Обновите stories
3. Запустите `npm run test:visual`
4. Если есть изменения - просмотрите их в Chromatic UI
5. Одобрите или отклоните изменения

### 2. Code Review

1. В PR автоматически появится комментарий от Chromatic с ссылкой на изменения
2. Reviewer может просмотреть визуальные изменения перед одобрением PR
3. Изменения должны быть одобрены в Chromatic перед мержем

### 3. CI/CD интеграция

Добавьте в ваш CI/CD pipeline (например, GitHub Actions):

```yaml
- name: Run Chromatic
  uses: chromaui/action@v1
  with:
    projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
    buildScriptName: build-storybook
```

## Полезные команды

```bash
# Запустить только определенные stories
npx chromatic --only-story-names="Card/Basic,Card/ProductCard"

# Игнорировать изменения и обновить baseline
npx chromatic --auto-accept-changes

# Запуск в debug режиме
npx chromatic --debug

# Локальный режим (не загружать в облако)
npx chromatic --dry-run
```

## Конфигурационный файл

Настройки в `chromatic.config.json`:

- `projectToken`: Токен проекта Chromatic
- `buildScriptName`: Название npm скрипта для сборки Storybook
- `storybookBuildDir`: Директория с собранным Storybook
- `exitZeroOnChanges`: Возвращать код 0 даже при наличии изменений
- `skip`: Пропускать определенные ветки (например, dependabot)
- `externals`: Внешние файлы для загрузки

## Troubleshooting

### Ошибка "Project token not found"
- Убедитесь, что токен правильно скопирован
- Проверьте, что нет лишних пробелов в токене
- Используйте переменные окружения для безопасности

### Медленная загрузка снимков
- Проверьте интернет-соединение
- Уменьшите количество stories для тестирования
- Используйте параметр `delay` для медленно загружающихся компонентов

### Ложные срабатывания
- Добавьте `delay` для анимированных компонентов
- Используйте `chromatic: { disable: true }` для нестабильных stories
- Настройте `threshold` для допустимых различий в пикселях

## Дополнительные ресурсы

- [Документация Chromatic](https://www.chromatic.com/docs/)
- [Storybook Visual Testing](https://storybook.js.org/docs/react/writing-tests/visual-testing)
- [Best Practices](https://www.chromatic.com/docs/best-practices)
